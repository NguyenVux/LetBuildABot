{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake: configure",
            "type": "cmake",
            "command": "configure",
            "problemMatcher": []
        },
        {
            "label": "CMake: build for launch",
            "type": "cmake",
            "command": "build",
            "targets": [
                "${command:cmake.launchTargetName}"
            ],
            "dependsOn": "CMake: configure",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
